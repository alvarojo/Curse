//This is a root advancement. It is the base advancement for a page. As such, it has no parent and has to set a background.

//Set icon item
setIcon("minecraft:clay")

//Set title String
setTitle("Come one come clay")

//Set description String
setDescription("You've managed to crash land on a simple, mOiSt planet. No water, no food, no real supplies. The first challenge, to survive the blazing mOiST where every drop of meme is precious.")

//Set background String. This is a path to any texture in a resource pack, so you can set it to fun things.
setBackground("minecraft:textures/gui/advancements/backgrounds/adventure.png")

//Set the toast notification in the top right corner of the screen to not show up when this advancement is achieved. 
setShowToast(true)

//Set the advancement to not send a chat message when it is achieved.
setAnnounceToChat(true)

//Adds a single location criteria. Since this has nothing specific set, it will be completed the instant the player is added to the world, thus unlocking the page instantly.
addCriteria("location", "minecraft:location")

//Sets the requirements to be any. This means that if ANY of the criteria triggers is acquired, the advancement is acquired.
setRequirements("any")

addRewardFunction("triumph:happy_birthday")